<div class="form-group row">
  <div class="form-group">
    <%= form_for(@user_favorite_route) do |f| %>
      <% if @user_favorite_route.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user_favorite_route.errors.count, "error") %> prohibited this user_favorite_route from being saved:</h2>
    
          <ul>
          <% @user_favorite_route.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    
      <div class="form-group col-lg-6 col-md-6 col-sm-6">
        <%= f.label :user_id %><br>
        <%= f.text_field :user_id, class: "form-control" %>
      </div>
      <div class="form-group col-lg-6 col-md-6 col-sm-6">
        <%= f.label :vehicle_route_id %><br>
        <%= f.text_field :vehicle_route_id, class: "form-control" %>
      </div>
      <div class="actions">
        <%= f.submit "create User favorite Route", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>