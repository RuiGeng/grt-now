<%= form_for(@users_favorite_route) do |f| %>
  <% if @users_favorite_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_favorite_route.errors.count, "error") %> prohibited this users_favorite_route from being saved:</h2>

      <ul>
      <% @users_favorite_route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :vehicle_route_id %><br>
    <%= f.text_field :vehicle_route_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
